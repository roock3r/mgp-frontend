import Wrapper from "./wrapper";
import Blocker from "./frontend/common/blocker";

function Root() {
    return (
        <Wrapper>
            <Blocker/>
        </Wrapper>
    );
}

export default Root;
